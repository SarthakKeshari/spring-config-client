services:
  - type: web
    name: spring-config-client
    env: java
    plan: free
    buildCommand: mvn clean install
    startCommand: java -jar build/libs/spring-config-client-0.0.1-SNAPSHOT.jar  # Replace with your actual JAR
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: dev
